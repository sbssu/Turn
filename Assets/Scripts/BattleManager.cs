using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleManager : MonoBehaviour
{
    public void StartBattle(string id, Character attacker, Character target)
    {
        switch(id)
        {
            case "ATTACK":

                break;
        }
    }
}
